import SectionHeader from "./SectionHeader";

const Resume: React.FC <{}> = () => {
  return (
    <div className="h-500" id="resume">
      <SectionHeader title={"Resume"} />
    </div>
  )
}

export default Resume;